# ==============================================================
# Задание 9.4
# С помощью map увеличить каждый элемент списка на 10.
# ==============================================================

numbers = [1, 2, 3, 4, 5]
result = list(map(lambda x: x + 10, numbers))

print(result)
